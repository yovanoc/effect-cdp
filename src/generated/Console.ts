// AUTO-GENERATED by scripts/codegen.ts — DO NOT EDIT
import { Schema } from "effect";

export const ConsoleMessage = Schema.Struct({
  source: Schema.Literals([
    "xml",
    "javascript",
    "network",
    "console-api",
    "storage",
    "appcache",
    "rendering",
    "security",
    "other",
    "deprecation",
    "worker",
  ]),
  level: Schema.Literals(["log", "warning", "error", "debug", "info"]),
  text: Schema.String,
  url: Schema.optional(Schema.String),
  line: Schema.optional(Schema.Number),
  column: Schema.optional(Schema.Number),
}).annotate({ identifier: "Console.ConsoleMessage" });

export const clearMessages = {
  method: "Console.clearMessages" as const,
  params: Schema.Struct({}).annotate({
    identifier: "Console.clearMessages.params",
  }),
  result: Schema.Struct({}).annotate({
    identifier: "Console.clearMessages.result",
  }),
} as const;

export const disable = {
  method: "Console.disable" as const,
  params: Schema.Struct({}).annotate({ identifier: "Console.disable.params" }),
  result: Schema.Struct({}).annotate({ identifier: "Console.disable.result" }),
} as const;

export const enable = {
  method: "Console.enable" as const,
  params: Schema.Struct({}).annotate({ identifier: "Console.enable.params" }),
  result: Schema.Struct({}).annotate({ identifier: "Console.enable.result" }),
} as const;

export const messageAdded = {
  method: "Console.messageAdded" as const,
  params: Schema.Struct({
    message: ConsoleMessage,
  }).annotate({ identifier: "Console.messageAdded.params" }),
} as const;
